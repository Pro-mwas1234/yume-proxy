{
  "name": "yumezone-worker",
  "version": "1.0.0",
  "description": "HLS Proxy Worker for Cloudflare",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "preview": "wrangler preview"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
